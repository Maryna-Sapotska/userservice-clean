spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///userservice
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
